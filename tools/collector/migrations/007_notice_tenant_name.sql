-- 매각물건명세서 점유자(임차인) 성명 저장 (WP-11 §3-4).
--
-- 005는 성명을 저장하지 않고 tenant_seq로만 익명화했다. 사용자 결정(2026-07-30)으로 성명 저장을
-- 허용한다 — 성명은 민감정보·고유식별정보가 아니고, 법원이 공개한 정보를 경매 참여자의 권리분석
-- 목적으로 쓰는 것이며 유료 경매정보 업체의 업계 표준과 같다.
--
-- tenant_seq는 그대로 둔다: 같은 임차인이 정보출처별(권리신고/현황조사/등기사항전부증명서)로
-- 여러 행에 나오므로 동일인을 묶는 순번이 계속 필요하다.
--
-- 정규식 이름 마스킹은 여전히 금지다(006 주석 참조 — 오탐·미탐 모두 심했다).
-- 저장하려면 원문 그대로, 가리려면 아예 저장하지 않는다.

ALTER TABLE auction_item_notice_tenant
  ADD COLUMN IF NOT EXISTS tenant_name TEXT;
